import RegisterForm from './registerForm.container';
import saga from './registerForm.sagas';
import reducer from './registerForm.reducer';

export const registerFormSaga = saga;
export const registerFormReducer = reducer;

export default RegisterForm;
