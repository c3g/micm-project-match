import Snackbar from './snackbar.container';
import reducer from './snackbar.reducer';
import saga from './snackbar.sagas';

export const snackbarReducer = reducer;
export const snackbarSaga = saga;

export default Snackbar;
